# Size, speed and Windows verification

Work item: https://github.com/kortexa-ai/sideleaf.desktop/issues/3

Reduce the measured package, startup, memory and editing costs while retaining
literal Markdown, anchored comments, undo and safe explicit saves. Profile the
pinned runtime separately from the editor before choosing changes. Compare the
same fixtures and process groups before and after optimization.

Expand packaged Windows testing through the user's authorized SSH access to
Scrappy. Scope the checkout, toolchain, fixtures, process actions and any test
automation to Sideleaf. Preserve Harmonizer and user documents. Record concrete
coverage and remaining limits in the issue and a dated verification document.
